#include<stdio.h>
#include<math.h>
long GiaiThua (int n)
{
    if(n == 0)
        return 1;
    return GiaiThu a(n - 1) * n;
}
int main()
{
    int n, T;
    printf("\Nhap n = "); scanf("%d", &n);
    T = GiaiThua (n);
    printf("T = %d", T);
    return 0;
}
